[project]
name = "contact-manager"
version = "0.1.0"
description = "Contact Manager App using PySide6 and ui_components"
requires-python = ">=3.9"
dependencies = [
    "PySide6>=6.5.0",
]

[project.scripts]
contact-manager = "main:main"
